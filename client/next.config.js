/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['m.media-amazon.com', 'learnbestcoding.com','localhost'],
  },

}

module.exports = nextConfig
